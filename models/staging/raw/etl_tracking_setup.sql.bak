{# Temporarily disabled to debug config undefined error
{{ config(
    materialized='table',
    tags=['etl', 'tracking', 'setup']
) }}

-- Only create tracking tables if explicitly requested
{% if var('create_tracking_tables', false) %}
    {{ create_etl_tracking_tables() }}
{% endif %}

-- Return a simple result to satisfy dbt
select 1 as setup_complete 
#}

-- Temporary placeholder
select 1 as setup_complete 
