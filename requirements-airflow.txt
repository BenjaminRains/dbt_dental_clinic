# Extra Python packages for Airflow Docker image (etl_pipeline DAG + dbt)
# Base image apache/airflow:2.7.1 already provides apache-airflow and core providers.
#
# Native host install (Option A): use requirements-airflow-native.txt instead.
# Do NOT pip install this file alone on the host — it omits apache-airflow and can pull Airflow 3.

# Business-hours guard (wall clock in America/Chicago)
pendulum>=2.0

# ETL pipeline runtime (DAG imports etl_pipeline in PythonOperators)
pyyaml>=6.0
psycopg2-binary>=2.9
pymysql>=1.1
sqlalchemy>=1.4,<2.0
python-dotenv>=1.0

# dbt (BashOperator runs dbt deps / dbt build in nightly DAG)
dbt-core>=1.6,<2
dbt-postgres>=1.6,<2

# Notifications (send_completion_notification uses SlackWebhookHook)
apache-airflow-providers-slack>=8.0
