# Native Airflow install (Option A / Phase A on host — not Docker image base).
# Install: pip install -r requirements-airflow-native.txt
# Requires Python 3.11.x (matches constraints-3.1.7).

-c https://raw.githubusercontent.com/apache/airflow/constraints-3.1.7/constraints-3.11.txt

apache-airflow==3.1.7

# Operators moved out of core in Airflow 3 (PythonOperator, BashOperator, etc.)
apache-airflow-providers-standard>=1.0.0

# FAB auth manager — keeps `airflow users create` + existing .env.native admin password flow
# (Simple Auth is Airflow 3 default; we opt into FAB for Option A continuity)
apache-airflow-providers-fab>=2.0.0

# DAG runtime (ETL imports, schema refresh, connections)
pyyaml>=6.0
psycopg2-binary>=2.9
pymysql>=1.1
python-dotenv>=1.0

# dbt runs via mdc subprocess (user pipenv), not in this venv — do not pip-install dbt here.

# Notifications
apache-airflow-providers-slack>=8.0

# Phase A tests
pytest>=7.0
